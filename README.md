## Setup

Virtual Environment:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Update Requirements: 
```bash
pip freeze > requirements.txt
```

How to run
```bash
python main.py --record <number> #100 by default
```


